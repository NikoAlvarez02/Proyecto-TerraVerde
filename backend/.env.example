# Django settings (.env example)

# SECURITY
SECRET_KEY=change-me-please
DEBUG=False

# Hosts and CSRF
# Comma-separated list of allowed hosts (no spaces)
ALLOWED_HOSTS=localhost,127.0.0.1,espacioterraverde.com.ar,www.espacioterraverde.com.ar,72.60.251.9

# Comma-separated list of trusted origins for CSRF (include scheme)
CSRF_TRUSTED_ORIGINS=http://localhost,http://127.0.0.1,http://72.60.251.9,https://espacioterraverde.com.ar,https://www.espacioterraverde.com.ar

# Database (defaults to SQLite if not set)
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=
# DB_USER=
# DB_PASSWORD=
# DB_HOST=
# DB_PORT=

# Static files deploy target (optional)
# STATIC_ROOT_DEPLOY=staticfiles

