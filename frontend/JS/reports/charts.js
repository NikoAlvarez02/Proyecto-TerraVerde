function createBarChart(ctxId, labels, data){
  // Placeholder: integra Chart.js en tu HTML y usa esta función
  console.log('BarChart', ctxId, labels, data);
}

function createLineChart(ctxId, labels, data){
  console.log('LineChart', ctxId, labels, data);
}

function createPieChart(ctxId, labels, data){
  console.log('PieChart', ctxId, labels, data);
}

function createAreaChart(ctxId, labels, data){
  console.log('AreaChart', ctxId, labels, data);
}

