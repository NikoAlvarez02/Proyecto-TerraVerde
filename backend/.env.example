# Copia este archivo como .env y completa los secretos.

# Django
SECRET_KEY=<poner_secret_key>
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1

# Base de datos PostgreSQL
DB_ENGINE=django.db.backends.postgresql
DB_NAME=centroterraverde
DB_USER=terraverde
DB_PASSWORD=<poner_password>
DB_HOST=localhost
DB_PORT=5432

# Email (ajusta según tu proveedor)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
EMAIL_HOST_USER=<tu_correo>
EMAIL_HOST_PASSWORD=<poner_password>
DEFAULT_FROM_EMAIL=<tu_correo>

# CORS / CSRF opcionales para producción
# CSRF_TRUSTED_ORIGINS=https://tu-dominio.com,https://www.tu-dominio.com
# CORS_ALLOWED_ORIGINS=https://tu-frontend.com

