name: Deploy to VPS

on:
  push:
    branches:
      - main # o la rama que uses para desplegar

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            # ----------------------------------------------------
            # Rutas críticas
            PROJECT_ROOT="/root/Proyecto-TerraVerde/backend"
            VENV_ACTIVATE="$PROJECT_ROOT/.venv/bin/activate"
            # ----------------------------------------------------
            
            # Navegar al directorio y detener el script ante cualquier error (CRÍTICO)
            cd $PROJECT_ROOT || exit 1
            set -e 

            echo "📦 Actualizando código..."
            git pull

            # Paso 1: CREAR EL VENV SI NO EXISTE
            if [ ! -f "$VENV_ACTIVATE" ]; then
              echo "🐍 El entorno virtual (.venv) no existe. Creándolo..."
              # Usamos 'python3' para garantizar que se usa la versión correcta al crear
              python3 -m venv .venv 
            fi

            echo "🐍 Activando entorno virtual..."
            # Se corrige la ruta y se usa 'source'
            source $VENV_ACTIVATE
            
            # NOTA: Los comandos 'pip' y 'python' ahora apuntan al VENV

            echo "🔧 Instalando dependencias..."
            pip install -r requirements.txt

            echo "🗃️ Aplicando migraciones..."
            # Usamos 'python' (ya activado en el venv)
            python manage.py migrate --noinput

            echo "📁 Recolectando archivos estáticos..."
            # Usamos 'python' (ya activado en el venv)
            python manage.py collectstatic --noinput

            echo "🔄 Reiniciando Gunicorn..."
            systemctl restart gunicorn

            echo "✅ Reinicio de Gunicorn completo"

            echo "🧱 (Opcional) Reiniciando Nginx..."
            systemctl reload nginx || echo "Nginx no recargado (no es crítico)"
